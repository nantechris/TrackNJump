/**
 * Environnement de développement.
 * Ce fichier est utilisé lors du build en mode développement.
 *
 * Le fichier environment.prod.ts remplace celui-ci lors d'un build de production
 * grâce à la configuration `fileReplacements` d'Angular.
 */
export const environment = {
  production: false,
};
